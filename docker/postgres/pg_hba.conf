# PostgreSQL Client Authentication Configuration File
# Тип подключения | База данных | Пользователь | Адрес | Метод аутентификации

# Локальные подключения
local   all             all                                     trust

# IPv4 локальные подключения
host    all             all             127.0.0.1/32            md5
host    all             all             0.0.0.0/0               md5

# IPv6 локальные подключения  
host    all             all             ::1/128                 md5

# Подключения из Docker сети
host    all             all             172.16.0.0/12           md5
host    all             all             192.168.0.0/16          md5
host    all             all             10.0.0.0/8              md5

# Внешние подключения (без SSL для простоты)
host    all             all             0.0.0.0/0               md5

# Репликация (если нужна)
host    replication     all             127.0.0.1/32            md5
host    replication     all             ::1/128                 md5
